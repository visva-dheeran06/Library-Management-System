# Library-Management-System
Java desktop app for managing library books and members. Implements custom data structures (HashMap, Stack, Priority Queue) instead of built-ins. Features priority-based waiting queue (Premium > Faculty > Student), automatic overdue fine calculation, auto-issue on return, and a most-borrowed leaderboard.
